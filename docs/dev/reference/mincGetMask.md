# Minc Masks

Either create a mask from a file name (by reading the volume) or pass
the vector along undisturbed.

## Usage

``` r
mincGetMask(mask)
```

## Arguments

- mask:

  Either the path to a mask file or a numeric vector representing the
  mask

## Value

a numeric mask vector
