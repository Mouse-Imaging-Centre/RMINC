# Check for RMINC objects

Checks if the current object is one produced by RMINC (currently
`mincList`, `mincSingleDim`, `mincMultiDim`)

## Usage

``` r
is.minc(x)
```

## Arguments

- x:

  the object of interest

## Value

logical whether or not the object is an RMINC object
