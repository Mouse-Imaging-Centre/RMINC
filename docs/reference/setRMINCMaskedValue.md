# Set Masked Value

Use this function to configure how masked values should be treated

## Usage

``` r
setRMINCMaskedValue(val = NA)
```

## Arguments

- val:

  The new value for RMINC_MASKED_VALUE, defaults to resetting the masked
  value to NA
